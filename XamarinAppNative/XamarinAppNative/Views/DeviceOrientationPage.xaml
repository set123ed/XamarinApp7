<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="XamarinAppNative.Views.OrientationPage"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    ios:Page.UseSafeArea="true"
    Title="Orientation">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>

            </Grid.RowDefinitions>
            <Label Text="Device Orientation"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" Grid.Row="0" TextColor="Black"/>
            <!--<Button Text="{Binding ButtonText}"
                    Command="{Binding Orientation}" Grid.Row="1" />
            <Label Text="{Binding LabelText}"
                   HorizontalOptions="Center" Grid.Row="2" TextColor="DarkOrange" />-->
        </Grid>  
</ContentPage>
